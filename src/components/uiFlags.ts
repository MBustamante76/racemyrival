/**
 * Prototype UI toggles. Flip these when a shelved surface should come back.
 * Prefer flags over deleting components so later product chrome can return cleanly.
 */
export const SHOW_APP_HEADER = false;
